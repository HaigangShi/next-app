{
  "name": "next-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "NODE_ENV=development next dev",
    "dev:test": "NODE_ENV=test next dev",
    "dev:pre": "NODE_ENV=pre next dev",
    "build": "next build",
    "build:test": "NODE_ENV=test next build",
    "build:pre": "NODE_ENV=pre next build",
    "start": "next start",
    "start:test": "NODE_ENV=test next start",
    "start:pre": "NODE_ENV=pre next start",
    "lint": "next lint",
    "prepare": "husky install"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "next": "15.1.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3",
    "eslint": "^9",
    "eslint-config-next": "15.1.0",
    "husky": "^8.0.0",
    "lint-staged": "^15.2.0",
    "postcss": "^8",
    "tailwindcss": "^3.4.1"
  },
  "lint-staged": {
    "src/**/*.{js,jsx,ts,tsx}": [
      "lint --fix",
      "git add"
    ]
  }
}
